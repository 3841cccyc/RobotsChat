---
phase: 03-user-interaction
plan: 01
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - backend/app/services/group_chat_service.py
  - backend/app/routers/group_chat.py
  - tests/test_mention.py
autonomous: true
requirements:
  - USER-01
  - USER-02
must_haves:
  truths:
    - "用户输入@机器人名时，被@的机器人会响应"
    - "用户输入@all或@所有人时，所有机器人都会响应"
    - "用户发送消息后，机器人自动参与对话（当被@时）"
    - "用户可以随时加入机器人讨论话题，无需等待"
  artifacts:
    - path: "backend/app/services/group_chat_service.py"
      provides: "@mention解析和响应筛选逻辑"
      contains: "_parse_mentions"
    - path: "backend/app/services/group_chat_service.py"
      provides: "add_user_message_stream方法修改"
      contains: "mentioned_bots"
    - path: "backend/app/services/group_chat_service.py"
      provides: "start_group_chat_stream方法修改"
      contains: "responding_bots"
  key_links:
    - from: "add_user_message_stream"
      to: "_parse_mentions"
      via: "self._parse_mentions(user_prompt, bots)"
      pattern: "调用.*解析@mentions"
    - from: "_parse_mentions"
      to: "responding_bots"
      via: "if mentioned_bots: responding_bots = mentioned_bots"
      pattern: "筛选.*响应机器人"
---

<objective>
实现用户@提及机制，支持@特定机器人或@所有人，并修改机器人响应流程

Purpose: 让用户可以通过@提及来触发特定机器人响应，或@所有人让所有机器人响应
Output: 完成@mention解析和响应筛选功能
</objective>

<context>
@D:\0正事\学习资料\testforcc\.planning\phases\03-user-interaction\03-CONTEXT.md

# Key Code Context

## Existing Flow (group_chat_service.py)

`add_user_message_stream` (line 481-584):
- 获取历史消息
- 添加用户消息到历史
- 保存用户消息到数据库
- 让所有机器人依次回复

`start_group_chat_stream` (line 349-452):
- 创建会话，保存用户消息
- 按顺序让每个机器人发言

## Router Entry Points

`/group-chat/message-stream` (group_chat.py line 265):
- 调用 `group_chat_service.add_user_message_stream`
- 当前让所有机器人响应

`/group-chat/start-stream` (group_chat.py line 132):
- 启动群聊，第一个机器人响应

## Bot Model Reference

从 models.py 可知 Bot 有属性:
- id: int
- name: str (机器人名称)
- system_prompt: str
- temperature: float
- max_tokens: int

</context>

<tasks>

<task type="auto">
  <name>Task 0: Create test file for @mention parsing</name>
  <files>D:\0正事\学习资料\testforcc\tests\test_mention.py</files>
  <action>
创建测试文件 `tests/test_mention.py`，包含以下测试用例（使用 pytest）:

```python
import pytest
from unittest.mock import MagicMock

# 导入待测试的函数（实现后）
# from backend.app.services.group_chat_service import GroupChatService


class MockBot:
    """模拟Bot对象"""
    def __init__(self, id: int, name: str):
        self.id = id
        self.name = name
        self.system_prompt = "You are a helpful assistant."
        self.temperature = 0.7
        self.max_tokens = 1000


class TestParseMentions:
    """测试@mention解析功能"""

    def test_specific_bot_mention(self):
        """测试@提及特定机器人"""
        # 创建模拟机器人和服务
        bots = [
            MockBot(1, "小爱"),
            MockBot(2, "小度"),
            MockBot(3, "Siri")
        ]
        # 测试 "@小爱 你好"
        # 预期: 返回 [bots[0]] (小爱)
        pass

    def test_all_mention(self):
        """测试@all和@所有人"""
        bots = [
            MockBot(1, "小爱"),
            MockBot(2, "小度"),
            MockBot(3, "Siri")
        ]
        # 测试 "@all" 和 "@所有人"
        # 预期: 返回所有机器人
        pass

    def test_case_insensitive(self):
        """测试@mention不区分大小写"""
        bots = [
            MockBot(1, "XiaoAi"),
            MockBot(2, "小爱")
        ]
        # 测试 "@XIAOAI", "@xiaoai", "@小爱"
        # 预期: 都能匹配到对应机器人
        pass

    def test_user_message_with_mention(self):
        """测试用户消息中包含@mention"""
        bots = [
            MockBot(1, "小爱同学"),
            MockBot(2, "小度助手")
        ]
        # 测试 "@小爱同学 你好呀" - 完整名称匹配
        # 测试 "@小爱 你好呀" - 模糊匹配
        # 预期: 都能匹配到对应机器人
        pass
```

这些测试用例先写stub，实现功能后再填充断言。
</action>
  <verify>
<automated>pytest tests/test_mention.py --collect-only</automated>
  </verify>
  <done>测试文件已创建，包含4个测试用例的stub</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add @mention parsing function</name>
  <files>D:\0正事\学习资料\testforcc\backend\app\services\group_chat_service.py</files>
  <behavior>
    - Test 1: "@小爱 你好" 返回 [小爱机器人]
    - Test 2: "@all" 返回所有机器人
    - Test 3: "@小爱" 和 "@XIAOAI" 都匹配 "小爱"
    - Test 4: "你好" (无@) 返回空列表
  </behavior>
  <action>
在 GroupChatService 类中添加 `_parse_mentions` 方法，实现@mention解析:

```python
def _parse_mentions(self, text: str, bots: List[Bot]) -> List[Bot]:
    """
    解析用户消息中的@mentions，返回被@的机器人列表

    支持:
    - @机器人名 (模糊匹配，不区分大小写)
    - @all / @ALL (所有机器人响应)
    - @所有人 (所有机器人响应)

    Args:
        text: 用户消息内容
        bots: 可用机器人列表

    Returns:
        被@的机器人列表，如果无@则返回空列表
    """
    if not text or not bots:
        return []

    mentioned_bots = []
    text_lower = text.lower()

    # 检查@all或@所有人
    if '@all' in text_lower or '@所有人' in text_lower:
        return bots  # 返回所有机器人

    # 匹配@后跟机器人名 (模糊匹配)
    # 正则: @(\w+) 匹配@后跟字母数字下划线
    import re
    mention_pattern = re.compile(r'@(\w+)', re.IGNORECASE)
    mentions = mention_pattern.findall(text)

    for mention in mentions:
        mention_lower = mention.lower()

        # 查找匹配的机器人（模糊匹配，名称包含mention或mention包含名称）
        for bot in bots:
            bot_name_lower = bot.name.lower()
            if (mention_lower in bot_name_lower or
                bot_name_lower in mention_lower or
                mention_lower == bot_name_lower):
                if bot not in mentioned_bots:
                    mentioned_bots.append(bot)

    return mentioned_bots
```

此函数将在消息处理流程中被调用，根据@mentions筛选响应的机器人。
</action>
  <verify>
<automated>pytest tests/test_mention.py::TestParseMentions::test_specific_bot_mention tests/test_mention.py::TestParseMentions::test_all_mention tests/test_mention.py::TestParseMentions::test_case_insensitive -v</automated>
  </verify>
  <done>@mention解析函数已添加，支持@all/@所有人触发所有机器人，支持模糊匹配机器人名称</done>
</task>

<task type="auto">
  <name>Task 2: Modify add_user_message_stream to filter bots by @mentions</name>
  <files>D:\0正事\学习资料\testforcc\backend\app\services\group_chat_service.py</files>
  <action>
修改 `add_user_message_stream` 方法（line 481-584）:

1. 在保存用户消息后，调用 `_parse_mentions` 解析@mentions
2. 如果有@mentions，只让被@的机器人响应
3. 如果没有@mentions但有用户消息，让所有机器人响应（保持现有行为）
4. 在流式响应中发送@mention解析结果

关键修改:
```python
# 在 "yield {"type": "user_message", ...}" 后添加:
# 解析@mentions
mentioned_bots = self._parse_mentions(user_prompt, bots)

# 发送mentions解析结果
yield {
    "type": "mentions_parsed",
    "mentioned_bot_names": [b.name for b in mentioned_bots],
    "total_bots": len(bots)
}

# 确定响应的机器人列表
if mentioned_bots:
    responding_bots = mentioned_bots  # 只让被@的机器人响应
else:
    responding_bots = bots  # 没有@时所有机器人响应

# 修改响应循环: for bot in bots: -> for bot in responding_bots:
```

注意：保持向后兼容，没有@时行为不变。
</action>
  <verify>
<automated>pytest tests/test_mention.py::TestParseMentions::test_user_message_with_mention -v</automated>
  </verify>
  <done>add_user_message_stream支持根据@mentions筛选响应机器人，无@时保持原有行为（所有机器人响应）</done>
</task>

<task type="auto">
  <name>Task 3: Modify start_group_chat_stream to filter bots by @mentions</name>
  <files>D:\0正事\学习资料\testforcc\backend\app\services\group_chat_service.py</files>
  <action>
修改 `start_group_chat_stream` 方法（line 349-452）:

在保存用户消息后，调用 `_parse_mentions` 解析@mentions:

```python
# 在 "messages.append({...user_prompt...})" 后添加:
# 解析@mentions
mentioned_bots = self._parse_mentions(user_prompt, bots)

# 发送mentions解析结果
yield {
    "type": "mentions_parsed",
    "mentioned_bot_names": [b.name for b in mentioned_bots],
    "total_bots": len(bots)
}

# 确定响应的机器人列表
if mentioned_bots:
    responding_bots = mentioned_bots
else:
    responding_bots = bots

# 修改响应循环: for bot in bots: -> for bot in responding_bots:
```
</action>
  <verify>
<automated>pytest tests/test_mention.py -v</automated>
  </verify>
  <done>start_group_chat_stream支持根据@mentions筛选响应机器人</done>
</task>

</tasks>

<verification>
Overall checks:
- @机器人名 可以触发特定机器人响应
- @all / @ALL / @所有人 可以触发所有机器人响应
- 模糊匹配: @小爱 可以匹配 "小爱同学"
- 无@时所有机器人响应（保持向后兼容）
- 前端能收到 mentions_parsed 事件
</verification>

<success_criteria>
1. 用户在群聊中输入"@机器人名"时，机器人能够识别并响应
2. 用户输入"@所有人"时，所有机器人都能识别
3. 用户发送消息后，机器人自动参与对话（当被@时）
4. 用户可以随时加入机器人讨论话题，无需等待
</success_criteria>

<output>
After completion, create `.planning/phases/03-user-interaction/03-01-SUMMARY.md`
</output>
